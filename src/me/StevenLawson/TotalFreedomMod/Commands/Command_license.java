//No longer existing
/*
package me.StevenLawson.TotalFreedomMod.Commands;

import me.StevenLawson.TotalFreedomMod.Commands.AdminLevel;
import me.StevenLawson.TotalFreedomMod.Commands.CommandParameters;
import me.StevenLawson.TotalFreedomMod.Commands.CommandPermissions;
import me.StevenLawson.TotalFreedomMod.Commands.SourceType;
import me.StevenLawson.TotalFreedomMod.Commands.TFM_Command;
import me.StevenLawson.TotalFreedomMod.TFM_Util;
import net.minecraft.util.org.apache.commons.lang3.ArrayUtils;
import net.minecraft.util.org.apache.commons.lang3.StringUtils;
import org.bukkit.ChatColor;
import org.bukkit.command.Command;
import org.bukkit.command.CommandSender;
import org.bukkit.entity.Player;

@CommandPermissions(level = AdminLevel.ALL, source = SourceType.BOTH)
@CommandParameters(
        description = "EXPLODINGFreedomMod Licenseing Information",
        aliases = "efmlicense", 
        usage = "/<command>")
public class Command_license extends TFM_Command
{
    @Override
    public boolean run(CommandSender sender, Player sender_p, Command cmd, String commandLabel, String[] args, boolean senderIsConsole)
    {
        TFM_Util.playerMsg(sender_p, "EXPLODINGFreedomMod License", ChatColor.GOLD);
        TFM_Util.playerMsg(sender_p, "The EXPLODINGFreedomMod is a closed-source plugin that only the owner and the certified EFM developers can edit.", ChatColor.AQUA);
        TFM_Util.playerMsg(sender_p, "If you wish to use the plugin on your server, you must give credit to EXPLODINGTNT001 as he is the EFM Creator.", ChatColor.AQUA);
        TFM_Util.playerMsg(sender_p, "In buildcreator.properties and buildcreator.defualt.properties you cannot change the EXPLODINGTNT001 name in there as he is the EFM creator and that will violate our License.", ChatColor.AQUA);
        TFM_Util.playerMsg(sender_p, "Also you cannot remove the /efm, /efmanage, /tfm or /license commands", ChatColor.AQUA);
        TFM_Util.playerMsg(sender_p, "The EXPLODINGFreedomMod cannot be distributed by anyone else but the Owner or the EFM Developers and can only be uploaded on github and not anyother file sharing sites.", ChatColor.AQUA);
        TFM_Util.playerMsg(sender_p, "You can change the EXPLODINGFreedomMod name in plugin.yml BUT you must not change or remove anything that shows the EFM Creator and Developers", ChatColor.AQUA);
        TFM_Util.playerMsg(sender_p, "Thank you for reading this and enjoy our plugin! - EXPLODINGFreedomMod Developers", ChatColor.GREEN);
       
        return true;
       }
}
*/